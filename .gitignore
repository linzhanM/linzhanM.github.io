# Operating systems
.DS_Store
._*
Thumbs.db
Desktop.ini

# iCloud placeholders
.*.icloud
*.icloud

# Editors and local agent settings
.idea/
.vscode/
.zed/
*.swp
*.swo
*.swn
*.sublime-workspace
.claude/settings.local.json
AGENTS.md

# Local environments and dependencies
.venv/
venv/
node_modules/
.npm/
__pycache__/
*.py[cod]

# Secrets
.env
.env.*
*.pem

# Logs, temporary files, and local scratch space
*.log
*.tmp
*.bak
*.orig
*.rej
scratch/
tmp/

# Static-site build artifacts
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
*.css.map
*.js.map

# Resume LaTeX intermediates
resources/resume/*.aux
resources/resume/*.fdb_latexmk
resources/resume/*.fls
resources/resume/*.out
resources/resume/*.synctex.gz
resources/resume/*.toc

# Local media project files (finished website assets remain tracked)
*.aep
*.fcpbundle
*.prproj
*.mov.sb-*

# Uncompressed rig originals, kept locally as a working backup.
# unimate/resources/glbs/ ships the meshopt + WebP builds (650 MB -> 48 MB);
# these are the sources they were built from. Deliberately NOT committed: the
# whole repo is the deployed artifact, and 657 MB of duplicates would put the
# published site at ~986 MB against GitHub Pages' 1 GB limit. They were once
# recoverable from commit b4ac4c8; that history was purged on 2026-08-06 to get
# the repo back under 1 GB, so the local folder is now the ONLY copy. Back it up
# off this machine before deleting anything in it.
unimate/resources/glbs-raw/
